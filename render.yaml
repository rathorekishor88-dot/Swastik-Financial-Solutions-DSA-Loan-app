services:
  - type: web
    name: swastik-financial-dsa-loan
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    branch: main
